-- TODO - those mappings are only aliases for bindings provided by Comment.nvim, not for the commands themselves
require('Comment').setup({
  opleader = {
    line = '<leader>/',
    block = '<leader>>'
  }
})
